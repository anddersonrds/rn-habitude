import { Animation } from "@expo/ui/swift-ui/modifiers";

/** How a native list settles when a row is added, removed or reordered. */
export const listChange = Animation.easeInOut({ duration: 0.22 });
