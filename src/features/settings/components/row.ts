/**
 * The corner the Android settings rows are cut to. A plain number rather than a
 * `Shapes.RoundedCorner`, so the label and the button that clips its ripple to
 * the same edge read it from one place without either importing the other's
 * platform file.
 */
export const SETTINGS_ROW_RADIUS = 20;
