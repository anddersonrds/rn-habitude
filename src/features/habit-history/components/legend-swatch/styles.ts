import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  legendItem: {
    flexDirection: "row",
    alignItems: "center",
    gap: 6,
  },
  legendSwatch: {
    width: 11,
    height: 11,
    borderRadius: 3,
  },
});
